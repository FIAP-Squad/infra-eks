apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: backend-payment
spec:
  generators:
    - list:
        elements:
          - cluster: local
            url: https://kubernetes.default.svc
            appName: backend-payment
            namespace: payment
  template:
    metadata:
      name: '{{appName}}'
    spec:
      project: fiap-project
      source:
        repoURL: git@github.com:FIAP-Squad/backend-payment.git
        targetRevision: main
        path: manifests/
      destination:
        server: '{{url}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true